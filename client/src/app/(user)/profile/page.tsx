import { redirect } from "next/navigation";

export default function ProfilePage() {
  // With parallel routing, we don't need to redirect to a specific route
  // The layout will handle showing the appropriate content
  return null;
}